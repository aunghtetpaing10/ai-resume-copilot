{
  "name": "@ai-resume-copilot/api",
  "version": "0.1.0",
  "private": true,
  "description": "Fastify REST API for AI Resume Copilot",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit"
  }
}
