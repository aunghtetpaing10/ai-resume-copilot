{
  "name": "@ai-resume-copilot/web",
  "version": "0.1.0",
  "private": true,
  "description": "React frontend for AI Resume Copilot",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .ts,.tsx",
    "type-check": "tsc --noEmit"
  }
}
